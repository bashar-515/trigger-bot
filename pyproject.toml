[project]
name = "trigger-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "viam-sdk>=0.47.0",
]

[dependency-groups]
lint = [
    "ruff>=0.12.0",
]
